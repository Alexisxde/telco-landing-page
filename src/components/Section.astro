---
const { title, className = '' } = Astro.props
---

<section
  class={`flex justify-center items-start flex-col min-w-[368px] max-w-[700px] m-5 mx-auto px-7 ${className}`}>
  {title && <h2 class="font-bold text-xl mb-2">{title}</h2>}
  <slot />
</section>
